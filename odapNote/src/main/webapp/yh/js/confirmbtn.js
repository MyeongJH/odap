;(function($){
  
  function clickHandler() {
    $(this).parents('.cbuton-cover').toggleClass('is_active');
  }

  $('.cbtn').on('click', clickHandler);

}(jQuery));

;(function($){
  
  function clickHandler() {
    $(this).parents('.cbutton-cover').toggleClass('is_active');
  }

  $('.cbtn').on('click', clickHandler);

}(jQuery));